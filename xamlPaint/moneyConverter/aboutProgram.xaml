<Page
    x:Class="moneyConverter.aboutProgram"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:moneyConverter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#B2BEB5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Waluty" Style="{ThemeResource BaseTextBlockStyle}" Margin="15 10 0 10"/>
            <TextBlock Text="About" Style="{ThemeResource HeaderTextBlockStyle}" HorizontalAlignment="Center" Margin="0 0 0 8"/>
        </StackPanel>
            <Button Content="Go back" Click="Button_Click"/>
        <Grid Grid.Row="1">
            <StackPanel Height="300">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    x:Name="Author"
                    Text="Autor: Filip Stachnik"
                    />
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    x:Name="Data"
                    />
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    x:Name="SourceValue"
                    />
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    x:Name="OutputValue"
                    />
            </StackPanel>

        </Grid>

    </Grid>
</Page>
